{
  "name": "cloudflare-worker-d1",
  "version": "1.0.0",
  "description": "Cloudflare Worker with D1 Database",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "itty-router": "^3.0.12"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
